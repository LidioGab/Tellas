{
  "name": "@stream-app/native-audio",
  "version": "1.0.0",
  "description": "Windows WASAPI Process Loopback Audio Capture with Process Exclusion (Discord)",
  "main": "index.js",
  "scripts": {
    "build": "node -e \"if(process.platform==='win32'){try{require('child_process').execSync('node-gyp rebuild',{stdio:'inherit'})}catch(e){}}\"",
    "clean": "node-gyp clean"
  },
  "dependencies": {
    "node-addon-api": "^8.0.0"
  },
  "devDependencies": {
    "node-gyp": "^10.0.0"
  }
}
